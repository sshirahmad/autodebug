# Sandbox Artifacts

## Regex Module Missing
The `regex` module (PyPI package) may not be installed in the sandbox even though it's a declared dependency of black. Install it with `pip install regex` before running tests/repro.
